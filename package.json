{
  "name": "quran.sh",
  "version": "0.6.0",
  "description": "A fast, offline-first Quran CLI and TUI reader",
  "type": "module",
  "bin": {
    "quran": "dist/index.js"
  },
  "files": [
    "dist/index.js",
    "dist/*.scm",
    "dist/*.wasm",
    "CHANGELOG.md",
    "LICENSE",
    "NOTICE",
    "README.md"
  ],
  "engines": {
    "bun": ">=1.3.0"
  },
  "keywords": [
    "quran",
    "islam",
    "cli",
    "tui",
    "terminal",
    "reader"
  ],
  "author": "smashah",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/smashah/quran.sh.git"
  },
  "bugs": "https://github.com/smashah/quran.sh/issues",
  "homepage": "https://github.com/smashah/quran.sh",
  "scripts": {
    "build": "bun build --minify --target bun --external @opentui/core --external @opentui/react --external react ./src/index.ts --outdir ./dist",
    "build:binary": "bun build --compile --minify --sourcemap --format esm ./src/index.ts --outfile ./dist/quran",
    "typecheck": "tsc --noEmit",
    "check": "bun run typecheck && bun test && bun run build && bun run build:binary && bun run smoke:builds",
    "prepublishOnly": "bun run typecheck && bun test && bun run build",
    "smoke:builds": "bun run src/dev/smoke-builds.ts",
    "test": "bun test"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@m31coding/fuzzy-search": "^2.0.0",
    "@opentui/core": "0.4.5",
    "@opentui/react": "0.4.5",
    "arabic-reshaper": "^1.1.0",
    "pngjs": "^7.0.0",
    "react": "^19.2.7"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/pngjs": "^6.0.5",
    "@types/react": "^19.2.17",
    "quran-json": "^3.1.2",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "shell-quote": "1.10.0",
    "ws": "8.21.1"
  }
}
